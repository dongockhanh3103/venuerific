default: &default
  adapter:  postgresql
  encoding: unicode
  host:     localhost
  username: admin
  password: root

development:
  <<: *default
  database: venuerific

production:
  <<: *default
  database: venuerific_test

test:
  <<: *default
  database: <%= ENV['DATABASE_URL'] %>
