default: &default
  adapter:  postgresql
  encoding: unicode
  host:     localhost
  username: root
  password: admin

development:
  <<: *default
  database: venuerific

production:
  <<: *default
  database: venuerific_test

test:
  <<: *default
  database: <%= ENV['DATABASE_URL'] %>

